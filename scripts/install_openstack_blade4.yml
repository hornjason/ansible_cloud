#!/usr/bin/ansible-playbook
---
# repave
 - hosts: [c00b04,c00b05,c00b06]
   gather_facts: no
   #roles:
   #  - { role: repave, tags: repave }
   #  - { role: prime_os, tags: prime_os }
   #  - { role: lab_custom, tags: lab_custom }
   tasks:
     - name: copy scripts
       copy: src={{ playbook_dir }}/scripts/{{ item }} dest=/root/
       with_items:
         - post_packstack.sh
         - answers.txt
         - openstack_test.sh
         - mitaka-rdo.sh

     - name: install packstack 
       shell: bash /root/mitaka-rdo.sh
        
     #- name: install openstack
     #  shell: packstack --answer-file=answers.txt
     #  when: inventory_hostname == "c00b04"

     - name: configure openstack
       shell: bash /root/post_packstack.sh
     
     - name: test openstack
       shell: . /root/keystone* ; bash /root/openstack_test.sh
       when: inventory_hostname == "c00b04"

